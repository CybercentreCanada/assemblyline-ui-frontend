# layout/routes

Routes layout that sets up the React Router `Routes` component with the application's route definitions. Maps route paths to their page components for navigation.

## Responsibilities

- React Router `Routes` setup with app route definitions
- Route-to-element mapping
- Route transition handling and layout wrappers

## Key Files

- `routes.layout.tsx` — Routes layout component with route mapping
