import type { CSSProperties } from 'react';

export const APP_DRAWER_WIDTHS: {
  xl: CSSProperties['width'];
  lg: CSSProperties['width'];
  md: CSSProperties['width'];
  sm: CSSProperties['width'];
  maximized: CSSProperties['width'];
} = {
  xl: '45vw',
  lg: '75%',
  md: '85%',
  sm: '100%',
  maximized: '90vw'
};
