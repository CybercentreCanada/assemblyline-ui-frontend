import { useAppPreferences } from 'app/layout.preferences';

/** @deprecated Use `useAppPreferences` from `app/layout.preferences` instead. */
export const useAppTemplatePreferences = useAppPreferences;
